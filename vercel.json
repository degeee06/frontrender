// vercel.json na RAIZ do projeto (não na pasta public)
{
  "routes": [
    {
      "src": "/",
      "dest": "/index.html"
    },
    {
      "src": "/auth/callback", 
      "dest": "/auth-callback.html"
    }
  ]
}
